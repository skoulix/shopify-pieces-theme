{%- comment -%}
  Container Class Helper
  Returns the appropriate container class based on full_width setting

  Usage:
    {% render 'container-class', full_width: section.settings.full_width %}

  Parameters:
    - full_width: Boolean - if true, returns 'page-full', else 'page-container'

  Output:
    Assigns 'container_class' variable for use in the calling template
{%- endcomment -%}

{%- liquid
  if full_width
    assign container_class = 'page-full'
  else
    assign container_class = 'page-container'
  endif
-%}
