{%- comment -%}
  Pieces Scroll Reveal Component

  A wrapper component for scroll-triggered reveal animations.

  Usage:
  {% render 'pieces-scroll-reveal',
    animation: 'fade-up',
    duration: 0.8,
    delay: 0,
    stagger: 0.1,
    start: 'top 85%',
    content: '<h2>My Content</h2>'
  %}

  Or wrap content directly in your templates:
  <c-scroll-reveal data-animation="fade-up" data-duration="0.8">
    <h2>My Content</h2>
  </c-scroll-reveal>

  Animation types:
  - fade
  - fade-up
  - fade-down
  - fade-left
  - fade-right
  - scale
  - scale-up
  - clip
  - clip-up
  - clip-down
{%- endcomment -%}

{%- liquid
  assign animation = animation | default: 'fade-up'
  assign duration = duration | default: 0.8
  assign delay = delay | default: 0
  assign stagger = stagger | default: 0
  assign start = start | default: 'top 85%'
-%}

<c-scroll-reveal
  data-animation="{{ animation }}"
  data-duration="{{ duration }}"
  data-delay="{{ delay }}"
  {% if stagger > 0 %}data-stagger="{{ stagger }}"{% endif %}
  data-start="{{ start }}"
>
  {{ content }}
</c-scroll-reveal>
