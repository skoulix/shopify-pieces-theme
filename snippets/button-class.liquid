{%- comment -%}
  Button Class Helper
  Calculates the appropriate button class based on style and color settings.

  Usage:
  {% render 'button-class', style: section.settings.button_style, color: section.settings.button_color %}

  Output: Sets `btn_class` variable in parent scope with the appropriate class.

  Parameters:
  - style: 'solid' or 'outline'
  - color: 'primary' or 'secondary'

  Result Classes:
  - solid + primary = btn--primary
  - solid + secondary = btn--solid-secondary
  - outline + primary = btn--outline
  - outline + secondary = btn--secondary
{%- endcomment -%}

{%- liquid
  if style == 'outline'
    if color == 'secondary'
      assign btn_class = 'btn--secondary'
    else
      assign btn_class = 'btn--outline'
    endif
  else
    if color == 'secondary'
      assign btn_class = 'btn--solid-secondary'
    else
      assign btn_class = 'btn--primary'
    endif
  endif
-%}
